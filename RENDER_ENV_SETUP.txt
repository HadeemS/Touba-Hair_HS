RENDER ENVIRONMENT VARIABLES SETUP
====================================

Go to: https://dashboard.render.com → Your Service → Environment

Set these EXACT variables:

1. MONGODB_URI
   Value:
   
   (The server will automatically add /touba-hair and retryWrites options)

2. JWT_SECRET
   Value: (Generate a random 32+ character string)
   
   To generate:
   - Go to: https://www.random.org/strings/?num=1&len=32&digits=on&upperalpha=on&loweralpha=on&unique=on&format=html&rnd=new
   - Or use: openssl rand -hex 32
   - Example: 

3. FRONTEND_URL
   Value: https://hadeems.github.io

4. NODE_ENV
   Value: production

After setting these:
1. Click "Save Changes"
2. Render will automatically redeploy
3. Wait 2-3 minutes for deployment
4. Test login again

